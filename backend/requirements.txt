# ====================================================================
# Paris United Group Holding - Backend Dependencies
# Phase 1: Project Foundation
# ====================================================================

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
gunicorn==23.0.0

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.13.3

# Validation and settings
pydantic==2.9.2
pydantic-settings==2.6.0
email-validator==2.2.0

# Environment
python-dotenv==1.0.1

# Security
bcrypt==4.2.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.12

# Phase 19 — rate limiting is implemented in-tree (app/core/rate_limit.py)
# as a FastAPI dependency. No third-party dependency required.

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# Phase 11 — CV parsing (text extraction from PDF / DOCX).
# OCR for scanned CVs is optional — install Tesseract + `pip install
# pytesseract Pillow` if you want it. The parser silently skips OCR
# when either dependency is missing.
pypdf==5.1.0
python-docx==1.1.2

# Post-launch — server-side image optimization: every uploaded
# image is resized to thumb / medium / large WebP variants so
# public pages serve a 1/3 - 1/10 size payload instead of the raw
# original. See app/services/image_optimization.py. Pillow doubles
# as the OCR dep for the CV parser above.
Pillow==10.4.0

# Phase 13 — Azure OpenAI client for the HR AI candidate review.
# Mock mode works without an Azure deployment; live mode requires the
# Azure-OpenAI endpoint + key set in .env.
openai==1.54.0

# Phase 16 — HR reports / exports. CSV uses the stdlib; Excel via
# openpyxl, PDF via reportlab.
openpyxl==3.1.5
reportlab==4.2.5

# Background scheduler — drives scheduled report digests and
# (future) nightly automatic database backups. Runs in-process
# inside the FastAPI lifespan; no external broker required.
APScheduler==3.10.4

# Digital Offers / Catalogue module — PDF -> WebP page rendering.
# PyMuPDF is the fastest pure-Python PDF rasteriser (faster than
# pdf2image which shells out to Poppler) and doesn't require a system
# binary, so it works on any container we deploy to.
PyMuPDF==1.24.10

# QR codes — branded PNGs for catalogue sharing. ``[pil]`` extra
# pulls in Pillow which is already required for image rebake +
# catalogue rendering.
qrcode[pil]==7.4.2

# Phase A-4 — structured logging. JSON output in production so log
# aggregators (Datadog, Loki, Cloudwatch) parse fields directly;
# colourised console output in development so a human can read a
# tail. Same ``get_logger()`` helper across the codebase.
structlog==24.4.0

# Phase A-6 — Cloudflare R2 object storage. R2 is S3-compatible so
# the standard ``boto3`` client works as-is once pointed at the R2
# endpoint URL. Used by ``app.services.storage`` to upload public
# media (CMS images + catalogue assets) to a CDN-fronted bucket
# instead of local disk. Local-disk fallback is preserved for
# unconfigured installs.
boto3==1.35.0

# Phase A-8 — Sentry error tracking. The ``[fastapi]`` extra pulls
# in starlette/asgi integration shims so ``sentry_sdk.init()`` is
# all the wiring we need — no explicit middleware registration.
# Inert when ``SENTRY_DSN`` is unset; behaviour-free in dev / tests.
sentry-sdk[fastapi]==2.18.0

# Phase B-1.0 — async DB drivers. Used by the new ``async_engine``
# and ``AsyncSessionLocal`` in ``app.core.database``, alongside the
# existing sync stack (psycopg2 + Session). New code can opt into
# async via ``Depends(get_async_db)``; nothing in the existing
# surface is migrated yet.
asyncpg==0.30.0
aiosqlite==0.20.0  # async SQLite driver for the test suite
